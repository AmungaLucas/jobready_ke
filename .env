DATABASE_URL=file:/home/z/my-project/db/custom.db
NEXTAUTH_SECRET=dev-secret-change-me-in-production
NEXTAUTH_URL=http://localhost:3000
# Set GEMINI_API_KEY to use the real Gemini 1.5 Flash API for CV/JD extraction.
# If unset, the platform uses a deterministic stub that produces the same JSON
# shape — useful for development without burning API tokens.
GEMINI_API_KEY=
